# Versioning

Use semantic versioning for published releases when the public API has compatibility guarantees.

- Patch: backwards-compatible fixes.
- Minor: backwards-compatible functionality.
- Major: breaking API changes.

Document breaking changes before release and keep the package version synchronized with release metadata.
