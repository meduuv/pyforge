# API contract

Public helpers should document accepted input types, return values, and exceptions.

When behavior changes, update the documentation and tests together. Prefer backwards-compatible additions over silently changing existing return formats.
